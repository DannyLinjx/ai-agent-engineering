"""Deprecated single-file Memory sample.

The generated package at ``agent_runtime.memory`` now provides the compatibility
facade and governed Memory contracts. This file remains in the template so existing
source manifests do not silently lose a known path.
"""
