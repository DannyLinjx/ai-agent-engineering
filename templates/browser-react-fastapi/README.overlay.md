# Browser experience for {{PROJECT_NAME}}

This project selected the governed React/Vite + FastAPI browser reference stack.
The Factory has composed this overlay without installing packages, starting services,
opening network ports, resolving credentials, or deploying the project.

The browser is a safe projection of server-owned state. It must not execute agent
logic, expose hidden reasoning, or decide authorization. Extend this overlay through
the generated Experience Manifest and keep approval, artifact, event, and Memory
boundaries explicit.
