interface:
  display_name: "AI Agent Engineering"
  short_description: "Build AI agents with optional channels, models, and MCP"
  brand_color: "#0F766E"
  default_prompt: "Use $ai-agent-engineering to build a secure AI agent while keeping channels, Model Providers, and MCP user-configurable and optional."
policy:
  allow_implicit_invocation: true
